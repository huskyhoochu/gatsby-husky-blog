import styled from 'styled-components';

const LeftSection = styled.section`
  border: 1px solid dodgerblue;
  position: sticky;
  top: 0;
  z-index: 1;
  height: 100vh;
  flex: 1;
`;

export default {
  LeftSection,
};
